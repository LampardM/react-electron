{
  "name": "react-electron",
  "version": "0.1.0",
  "private": true,
  "main": "main.js",
  "homepage": ".",
  "dependencies": {
    "@material-ui/core": "^4.4.2",
    "electron": "^6.0.10",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-scripts": "3.1.1"
  },
  "scripts": {
    "start": "export NODE_ENV=env && react-scripts start",
    "build": "export NODE_ENV=prod && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "estart": "export NODE_ENV=env && electron ."
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "node-sass": "^7.0.0",
    "react-router-dom": "^5.0.1",
    "sass-loader": "^8.0.0"
  }
}
